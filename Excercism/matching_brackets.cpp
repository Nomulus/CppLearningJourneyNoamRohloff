#include "matching_brackets.h"

namespace matching_brackets
{

    // TODO: add your solution here
    bool check(std::string bracket_test)
    {
        std::vector<char> brackets{};

        for (char character : bracket_test)
        {
            if (character = '(')
            {
            }
            if (character = '[')
            {
            }
            if (character = '{')
            {
            }
        }
    }

} // namespace matching_brackets
